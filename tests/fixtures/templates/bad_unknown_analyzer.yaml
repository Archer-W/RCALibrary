meta:
  id: test.bad-analyzer
  name: Bad Analyzer
data_pulls:
  - id: d1
    query: { dataset: ds1 }
analysis:
  - id: a1
    analyzer: this_analyzer_does_not_exist
    inputs: { dataset: d1 }
report:
  panels: []
