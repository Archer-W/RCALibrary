"""Co-located use-case plugins for this repo (loaded via RCA_PLUGINS).

These hold workflow ANALYSIS logic for specific RCA problems. The data SOURCE is
the swap point for the data agent (dummy CSV now -> real Snowflake later) — see
each template's IMPLEMENTATION.md.
"""
